"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';export default function InventoryMovementHistory({productId}:{productId?:string}){const s=createClient();const[r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{let q=s.from('inventory_movements').select('movement_type,quantity,unit_cost,reference,created_at').order('created_at',{ascending:false}).limit(100);if(productId)q=q.eq('inventory_product_id',productId);const{data}=await q;setR(data??[])})()},[productId]);return <div className="card"><div className="section-head"><h2>Movimientos de inventario</h2></div><div className="table-wrap"><table className="table"><thead><tr><th>Fecha</th><th>Tipo</th><th>Cantidad</th><th>Costo</th><th>Referencia</th></tr></thead><tbody>{r.map((x,i)=><tr key={i}><td>{new Date(x.created_at).toLocaleString('es-CO')}</td><td>{x.movement_type}</td><td>{x.quantity}</td><td>{x.unit_cost}</td><td>{x.reference??'—'}</td></tr>)}</tbody></table></div></div>}
