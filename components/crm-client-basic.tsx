"use client";import{useEffect,useState}from"react";import{MessageCircle}from"lucide-react";import{createClient}from"@/lib/supabase/client";import{getCurrentOrganizationId}from"@/lib/motomil/organization";type C={id:string;full_name:string;phone:string|null;whatsapp:string|null};export default function(){const s=createClient();const[r,setR]=useState<C[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from("customers").select("id,full_name,phone,whatsapp").eq("organization_id",o).eq("active",true).order("full_name");setR(data??[])})()},[]);return <div className="card"><div className="section-head"><h2>Clientes activos</h2><MessageCircle size={16}/></div><table className="table"><thead><tr><th>Cliente</th><th>Teléfono</th><th>WhatsApp</th></tr></thead><tbody>{r.map(x=><tr key={x.id}><td>{x.full_name}</td><td>{x.phone??"—"}</td><td>{x.whatsapp??"—"}</td></tr>)}</tbody></table></div>;}