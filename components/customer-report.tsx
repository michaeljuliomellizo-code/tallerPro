"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';import{getCurrentOrganizationId}from'@/lib/motomil/organization';export default function CustomerReport(){const s=createClient();const[r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from('customers').select('full_name,phone,email,active,created_at').eq('organization_id',o).order('full_name');setR(data??[])})()},[]);return <div className="card"><div className="section-head"><h2>Clientes</h2></div><div className="table-wrap"><table className="table"><thead><tr><th>Nombre</th><th>Teléfono</th><th>Correo</th><th>Activo</th></tr></thead><tbody>{r.map((x,i)=><tr key={i}><td>{x.full_name}</td><td>{x.phone??'—'}</td><td>{x.email??'—'}</td><td>{x.active?'Sí':'No'}</td></tr>)}</tbody></table></div></div>}
