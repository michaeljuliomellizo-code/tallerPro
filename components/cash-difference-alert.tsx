import{money}from'@/lib/motomil/formatters';export default function CashDifferenceAlert({difference}:{difference:number}){if(difference===0)return null;return <div className="card" style={{border:'1px solid #efd28a',background:'#fff8e8'}}><strong>Diferencia de caja: {money(difference)}</strong><div className="muted" style={{marginTop:4}}>Registra la explicación correspondiente en el cierre.</div></div>}
