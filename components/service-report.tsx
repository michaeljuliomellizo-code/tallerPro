"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';import{getCurrentOrganizationId}from'@/lib/motomil/organization';import{money}from'@/lib/motomil/formatters';export default function ServiceReport(){const s=createClient();const[r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from('service_orders').select('order_number,status,total,created_at').eq('organization_id',o).order('created_at',{ascending:false}).limit(100);setR(data??[])})()},[]);return <div className="card"><div className="section-head"><h2>Servicios</h2></div><div className="table-wrap"><table className="table"><thead><tr><th>Orden</th><th>Estado</th><th>Total</th></tr></thead><tbody>{r.map(x=><tr key={x.order_number}><td>#{x.order_number}</td><td>{x.status}</td><td>{money(x.total)}</td></tr>)}</tbody></table></div></div>}
