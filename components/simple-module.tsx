import { BarChart3, CheckCircle2, Database, ExternalLink, FileCog, MessageCircle, Plus, Settings2, Sparkles, WalletCards } from "lucide-react";
export function SimpleModule({eyebrow,title,subtitle,metrics,columns,rows,action="Nuevo registro",icon="data",children}:{eyebrow:string,title:string,subtitle:string,metrics?:Array<{label:string,value:string,note?:string,tone?:string}>,columns?:string[],rows?:Array<string[]>,action?:string,icon?:string,children?:React.ReactNode}){const Icon=icon==="message"?MessageCircle:icon==="chart"?BarChart3:icon==="wallet"?WalletCards:icon==="spark"?Sparkles:icon==="settings"?Settings2:icon==="api"?FileCog:Database;return <><div className="section-head"><div><div className="eyebrow">{eyebrow}</div><h1 className="page-title">{title}</h1><p className="page-subtitle">{subtitle}</p></div><button className="btn btn-primary"><Plus size={15}/>{action}</button></div>{metrics&&<div className="grid grid-4">{metrics.map((m,i)=><div className={`card metric ${m.tone||""}`} key={m.label}><div className="label">{m.label}</div><div className="value">{m.value}</div><div className="delta">{m.note||""}</div></div>)}</div>}<div style={{height:16}}/>{children||<div className="card"><div className="section-head"><h2>Registros</h2><Icon size={16}/></div>{columns&&rows&&<div className="table-wrap"><table className="table"><thead><tr>{columns.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((c,j)=><td key={j}>{j===0?<strong>{c}</strong>:c}</td>)}</tr>)}</tbody></table></div>} {!rows?.length&&<div className="empty">Aún no hay registros. Crea el primero para comenzar.</div>}</div>}</>}
export function FeatureList({items}:{items:Array<{title:string,detail:string,done?:boolean}>}){return <div className="grid" style={{gap:9}}>{items.map(x=><div className="alert" key={x.title}><span className="dot"/><div><strong>{x.title}</strong><small>{x.detail}</small></div>{x.done&&<CheckCircle2 size={15} color="#0c6b58"/>}</div>)}</div>}
