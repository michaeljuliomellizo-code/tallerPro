"use client";import{useState}from'react';import{ROLE_LABELS,ROLES}from'@/lib/motomil/roles';export default function UserForm({onClose,onSaved}:{onClose:()=>void;onSaved:()=>void}){const[f,setF]=useState({email:'',role:'reception'});return <form className="card" onSubmit={e=>{e.preventDefault();onSaved()}}><div className="section-head"><h2>Invitar usuario</h2></div><div className="form-grid"><div className="field"><label>Correo *</label><input required type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></div><div className="field"><label>Rol</label><select value={f.role} onChange={e=>setF({...f,role:e.target.value})}>{ROLES.filter(x=>x!=='customer').map(x=><option key={x} value={x}>{ROLE_LABELS[x]}</option>)}</select></div></div><div className="muted" style={{marginTop:10}}>La creación final del usuario requiere integrar una Edge Function o flujo de invitación de Supabase Auth.</div><div style={{display:'flex',justifyContent:'flex-end',gap:8,marginTop:16}}><button type="button" className="btn btn-ghost" onClick={onClose}>Cancelar</button><button className="btn btn-primary">Guardar invitación</button></div></form>}
