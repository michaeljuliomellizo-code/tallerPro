"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';import{getCurrentOrganizationId}from'@/lib/motomil/organization';import{money,dateTime}from'@/lib/motomil/formatters';
export default function SalesReport(){const s=createClient();const[r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from('pos_sales').select('sale_number,total,status,created_at').eq('organization_id',o).order('created_at',{ascending:false}).limit(100);setR(data??[])})()},[]);return <div className="card"><div className="section-head"><div><div className="eyebrow">Reporte</div><h2>Ventas POS</h2></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Venta</th><th>Fecha</th><th>Total</th><th>Estado</th></tr></thead><tbody>{r.map(x=><tr key={x.sale_number+'-'+x.created_at}><td>POS-{String(x.sale_number).padStart(6,'0')}</td><td>{dateTime(x.created_at)}</td><td>{money(x.total)}</td><td>{x.status}</td></tr>)}</tbody></table></div></div>}
