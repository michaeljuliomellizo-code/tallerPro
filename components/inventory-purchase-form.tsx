"use client";import{useState}from'react';export default function InventoryPurchaseForm({onClose}:{onClose:()=>void}){const[f,setF]=useState({supplier:'',reference:'',notes:''});return <form className="card" onSubmit={e=>{e.preventDefault();onClose()}}><div className="section-head"><div><div className="eyebrow">Inventario</div><h2>Nueva compra</h2></div></div><div className="form-grid"><div className="field"><label>Proveedor</label><input value={f.supplier} onChange={e=>setF({...f,supplier:e.target.value})}/></div><div className="field"><label>Referencia</label><input value={f.reference} onChange={e=>setF({...f,reference:e.target.value})}/></div><div className="field field-full"><label>Notas</label><textarea rows={3} value={f.notes} onChange={e=>setF({...f,notes:e.target.value})}/></div></div><div className="muted" style={{marginTop:12}}>La recepción detallada de líneas debe conectarse al módulo de compras cuando exista la tabla de órdenes de compra.</div><div style={{display:'flex',justifyContent:'flex-end',gap:8,marginTop:16}}><button type="button" className="btn btn-ghost" onClick={onClose}>Cancelar</button><button className="btn btn-primary">Guardar borrador</button></div></form>}
