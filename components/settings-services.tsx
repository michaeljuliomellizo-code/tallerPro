"use client";import{useState}from'react';export default function SettingsServices(){const[r,setR]=useState(['Mantenimiento general','Cambio de aceite','Diagnóstico']);const[x,setX]=useState('');return <div className="card"><div className="section-head"><h2>Servicios</h2></div><div style={{display:'flex',gap:8}}><input value={x} onChange={e=>setX(e.target.value)} placeholder="Nuevo servicio"/><button className="btn btn-primary" onClick={()=>{if(x.trim()){setR([...r,x.trim()]);setX('')}}}>Agregar</button></div><div style={{display:'grid',gap:8,marginTop:12}}>{r.map((v,i)=><div className="card" key={i}>{v}</div>)}</div></div>}
