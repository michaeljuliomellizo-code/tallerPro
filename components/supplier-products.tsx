"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';export default function SupplierProducts({supplierId}:{supplierId:string}){const s=createClient();const[r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await s.from('inventory_products').select('sku,name,stock,cost,sale_price,active').eq('supplier_id',supplierId).order('name');setR(data??[])})()},[supplierId]);return <div className="card"><div className="section-head"><h2>Productos del proveedor</h2></div><div className="table-wrap"><table className="table"><thead><tr><th>SKU</th><th>Producto</th><th>Stock</th><th>Costo</th><th>Venta</th></tr></thead><tbody>{r.map(x=><tr key={x.sku}><td>{x.sku}</td><td>{x.name}</td><td>{x.stock}</td><td>{x.cost}</td><td>{x.sale_price}</td></tr>)}</tbody></table></div></div>}
