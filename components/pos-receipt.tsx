export default function PosReceipt({sale,items}:{sale:any;items:any[]}){return <div className="card" id="pos-receipt"><div style={{textAlign:'center'}}><strong>MotoMil</strong><div className="muted">Comprobante POS-{String(sale.sale_number).padStart(6,'0')}</div></div><div className="table-wrap" style={{marginTop:12}}><table className="table"><tbody>{items.map(i=><tr key={i.id}><td>{i.description} × {i.quantity}</td><td style={{textAlign:'right'}}>{Number(i.total).toLocaleString('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0})}</td></tr>)}</tbody></table></div><div style={{display:'flex',justifyContent:'space-between',fontSize:18,marginTop:12}}><strong>Total</strong><strong>{Number(sale.total).toLocaleString('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0})}</strong></div></div>}
