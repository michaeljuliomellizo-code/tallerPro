import type{OrderStatus}from"@/lib/motomil/types";import{isClosed,canQuote,canApprove,canRepair}from"@/lib/motomil/guards";export default function({status}:{status:OrderStatus}){return <div className="card"><h2>Reglas de negocio</h2><div>Cerrada: {isClosed(status)?"Sí":"No"}</div><div>Puede cotizar: {canQuote(status)?"Sí":"No"}</div><div>Puede aprobar: {canApprove(status)?"Sí":"No"}</div><div>Puede reparar: {canRepair(status)?"Sí":"No"}</div></div>}