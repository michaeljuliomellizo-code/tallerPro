import{money}from'@/lib/motomil/formatters';export default function CashClosingSummary({expected,counted}:{expected:number;counted:number}){return <div className="grid grid-3"><div className="card"><div className="muted">Esperado</div><strong>{money(expected)}</strong></div><div className="card"><div className="muted">Contado</div><strong>{money(counted)}</strong></div><div className="card"><div className="muted">Diferencia</div><strong style={{color:counted-expected===0?'#0c6b58':'#a52222'}}>{money(counted-expected)}</strong></div></div>}
