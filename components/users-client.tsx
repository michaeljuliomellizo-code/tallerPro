"use client";import{useState}from'react';import UserForm from'./user-form';export default function UsersClient(){const[o,setO]=useState(false);return <><div className="section-head"><div><div className="eyebrow">Seguridad</div><h1 className="page-title">Usuarios</h1><p className="page-subtitle">Roles y acceso a MotoMil.</p></div><button className="btn btn-primary" onClick={()=>setO(true)}>Invitar usuario</button></div><div className="card"><div className="muted">La administración de usuarios Auth requiere integración segura con el servidor.</div></div>{o&&<div style={{position:'fixed',inset:0,background:'rgba(0,0,0,.45)',display:'flex',alignItems:'center',justifyContent:'center',padding:20,zIndex:1000}}><div style={{width:'min(620px,100%)'}}><UserForm onClose={()=>setO(false)} onSaved={()=>setO(false)}/></div></div>}</>}
