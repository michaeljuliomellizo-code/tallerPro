"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';import{getCurrentOrganizationId}from'@/lib/motomil/organization';export default function InventoryLowStock(){const s=createClient();const[r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from('inventory_products').select('sku,name,stock,minimum_stock').eq('organization_id',o).eq('active',true);setR((data??[]).filter(x=>Number(x.stock)<Number(x.minimum_stock)))})()},[]);return <div className="card"><div className="section-head"><h2>Stock bajo</h2></div>{r.length?<div className="table-wrap"><table className="table"><thead><tr><th>SKU</th><th>Producto</th><th>Stock</th><th>Mínimo</th></tr></thead><tbody>{r.map(x=><tr key={x.sku}><td>{x.sku}</td><td>{x.name}</td><td>{x.stock}</td><td>{x.minimum_stock}</td></tr>)}</tbody></table></div>:<div className="muted">Sin productos bajo mínimo.</div>}</div>}
