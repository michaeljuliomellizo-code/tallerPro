"use client";import Link from'next/link';import{CalendarDays,ClipboardList,FileText,Wrench}from'lucide-react';export default function PortalDashboard({summary}:{summary:{motorcycles:number;appointments:number;orders:number;quotes:number}}){const A=[['Motocicletas',summary.motorcycles,'/portal/motocicletas',Wrench],['Citas',summary.appointments,'/portal/citas',CalendarDays],['Órdenes',summary.orders,'/portal/ordenes',ClipboardList],['Cotizaciones',summary.quotes,'/portal/cotizaciones',FileText]] as const;return <div className="grid grid-2">{A.map(([t,n,h,I])=><Link key={h} href={h} className="card" style={{textDecoration:'none'}}><div className="muted">{t}</div><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><strong style={{fontSize:30}}>{n}</strong><I size={25} style={{opacity:.45}}/></div></Link>)}</div>}
