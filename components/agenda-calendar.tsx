"use client";import{useMemo}from'react';export default function AgendaCalendar({appointments,onSelect}:{appointments:any[];onSelect:(a:any)=>void}){const days=useMemo(()=>{const m=new Map<string,any[]>();for(const a of appointments){const k=new Date(a.starts_at).toISOString().slice(0,10);m.set(k,[...(m.get(k)??[]),a])}return [...m.entries()]},[appointments]);return <div className="card"><div className="section-head"><div><div className="eyebrow">Calendario</div><h2>Agenda</h2></div></div>{days.length?<div className="grid grid-3">{days.map(([day,items])=><div className="card" key={day}><strong>{new Date(day+'T00:00:00').toLocaleDateString('es-CO',{weekday:'long',day:'2-digit',month:'short'})}</strong><div style={{display:'grid',gap:8,marginTop:10}}>{items.map(a=><button key={a.id} className="btn btn-ghost" style={{textAlign:'left'}} onClick={()=>onSelect(a)}>{new Date(a.starts_at).toLocaleTimeString('es-CO',{hour:'2-digit',minute:'2-digit'})} · {a.service_type??'Servicio'}</button>)}</div></div>)}</div>:<div className="muted">No hay citas.</div>}</div>}
