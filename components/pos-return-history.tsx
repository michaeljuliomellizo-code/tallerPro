"use client";export default function PosReturnHistory({returns}:{returns:any[]}){return <div className="card"><div className="section-head"><h2>Historial de devoluciones</h2></div>{returns.length?<div className="table-wrap"><table className="table"><thead><tr><th>Devolución</th><th>Fecha</th><th>Motivo</th><th>Total</th></tr></thead><tbody>{returns.map(r=><tr key={r.id}><td>DEV-{String(r.return_number).padStart(6,'0')}</td><td>{new Date(r.created_at).toLocaleString('es-CO')}</td><td>{r.reason}</td><td>{Number(r.total).toLocaleString('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0})}</td></tr>)}</tbody></table></div>:<div className="muted">No hay devoluciones.</div>}</div>}
