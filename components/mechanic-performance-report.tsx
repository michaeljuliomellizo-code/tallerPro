"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';import{getCurrentOrganizationId}from'@/lib/motomil/organization';export default function MechanicPerformanceReport(){const s=createClient();const[r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from('service_orders').select('mechanic_id,status,total').eq('organization_id',o);const m:any={};for(const x of data??[]){const k=x.mechanic_id??'sin-asignar';m[k]??={orders:0,delivered:0,total:0};m[k].orders++;if(x.status==='delivered')m[k].delivered++;m[k].total+=Number(x.total||0)}setR(Object.entries(m).map(([k,v]:any)=>({mechanic:k,...v})))})()},[]);return <div className="card"><div className="section-head"><h2>Rendimiento de mecánicos</h2></div><div className="table-wrap"><table className="table"><thead><tr><th>Mecánico</th><th>Órdenes</th><th>Entregadas</th><th>Total</th></tr></thead><tbody>{r.map(x=><tr key={x.mechanic}><td>{x.mechanic}</td><td>{x.orders}</td><td>{x.delivered}</td><td>{Number(x.total).toLocaleString('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0})}</td></tr>)}</tbody></table></div></div>}
