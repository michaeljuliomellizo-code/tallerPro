"use client";import{useEffect,useState}from"react";import Link from"next/link";import{Eye}from"lucide-react";import{createClient}from"@/lib/supabase/client";import{getCurrentOrganizationId}from"@/lib/motomil/organization";type O={id:string;order_number:number;status:string;mileage:number;reported_problem:string|null;received_at:string};export default function(){const s=createClient();const[r,setR]=useState<O[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from("service_orders").select("id,order_number,status,mileage,reported_problem,received_at").eq("organization_id",o).eq("status","received").order("received_at",{ascending:false});setR(data??[])})()},[]);return <div className="card"><div className="table-wrap"><table className="table"><thead><tr><th>Orden</th><th>Fecha</th><th>Km</th><th>Problema</th><th/></tr></thead><tbody>{r.map(x=><tr key={x.id}><td>#{x.order_number}</td><td>{new Date(x.received_at).toLocaleString("es-CO")}</td><td>{x.mileage.toLocaleString("es-CO")}</td><td>{x.reported_problem??"—"}</td><td><Link className="btn btn-ghost" href={`/ordenes/${x.id}`}><Eye size={13}/>Abrir</Link></td></tr>)}</tbody></table></div></div>;}