"use client";import{useEffect,useState}from"react";import Link from"next/link";import{FileText}from"lucide-react";import{createClient}from"@/lib/supabase/client";import{getCurrentOrganizationId}from"@/lib/motomil/organization";type Q={id:string;order_number:number;status:string;quoted_at:string|null;total:number};export default function(){const s=createClient();const[r,setR]=useState<Q[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from("service_orders").select("id,order_number,status,quoted_at,total").eq("organization_id",o).in("status",["quote","approved","cancelled"]).order("quoted_at",{ascending:false,nullsFirst:false});setR(data??[])})()},[]);return <div className="card">{r.length?<table className="table"><thead><tr><th>Orden</th><th>Estado</th><th>Fecha</th><th>Total</th><th/></tr></thead><tbody>{r.map(x=><tr key={x.id}><td>#{x.order_number}</td><td>{x.status}</td><td>{x.quoted_at?new Date(x.quoted_at).toLocaleString("es-CO"):"—"}</td><td>{Number(x.total).toLocaleString("es-CO",{style:"currency",currency:"COP",maximumFractionDigits:0})}</td><td><Link className="btn btn-ghost" href={`/ordenes/${x.id}`}><FileText size={13}/>Abrir</Link></td></tr>)}</tbody></table>:<div className="empty" style={{padding:30,textAlign:"center"}}>No hay cotizaciones.</div>}</div>;}