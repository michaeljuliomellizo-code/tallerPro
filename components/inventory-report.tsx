"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';import{getCurrentOrganizationId}from'@/lib/motomil/organization';import{money}from'@/lib/motomil/formatters';export default function InventoryReport(){const s=createClient();const[r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const o=await getCurrentOrganizationId();const{data}=await s.from('inventory_products').select('sku,name,stock,minimum_stock,cost,sale_price').eq('organization_id',o).eq('active',true).order('name');setR(data??[])})()},[]);return <div className="card"><div className="section-head"><h2>Inventario</h2></div><div className="table-wrap"><table className="table"><thead><tr><th>SKU</th><th>Producto</th><th>Stock</th><th>Mín.</th><th>Valor</th></tr></thead><tbody>{r.map(x=><tr key={x.sku}><td>{x.sku}</td><td>{x.name}</td><td>{x.stock}</td><td>{x.minimum_stock}</td><td>{money(Number(x.stock)*Number(x.cost))}</td></tr>)}</tbody></table></div></div>}
