import{appointmentStatusLabel}from'./agenda-status';export default function AppointmentDetail({appointment}:{appointment:any}){return <div className="card"><div className="section-head"><div><div className="eyebrow">Cita</div><h2>Detalle</h2></div></div><div className="grid grid-2"><div><div className="muted">Inicio</div><strong>{new Date(appointment.starts_at).toLocaleString('es-CO')}</strong></div><div><div className="muted">Fin</div><strong>{appointment.ends_at?new Date(appointment.ends_at).toLocaleString('es-CO'):'—'}</strong></div><div><div className="muted">Estado</div><span className="badge">{appointmentStatusLabel(appointment.status)}</span></div><div><div className="muted">Servicio</div><strong>{appointment.service_type??'—'}</strong></div></div>{appointment.notes&&<div style={{marginTop:12}}><div className="muted">Notas</div><div>{appointment.notes}</div></div>}</div>}
