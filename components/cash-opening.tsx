"use client";import{useState}from'react';import{createClient}from'@/lib/supabase/client';import{getCurrentOrganizationId}from'@/lib/motomil/organization';export default function CashOpening({onSaved,onClose,suggested=0}:{onSaved:()=>void;onClose:()=>void;suggested?:number}){const s=createClient();const[a,setA]=useState(String(suggested));const[n,setN]=useState('');const[e,setE]=useState('');async function save(ev:React.FormEvent){ev.preventDefault();try{const o=await getCurrentOrganizationId();const{error}=await s.rpc('open_cash_register',{p_organization_id:o,p_opening_amount:Number(a),p_notes:n.trim()||null});if(error)throw error;onSaved()}catch(x){setE(x instanceof Error?x.message:'No fue posible abrir la caja.')}}return <form className="card" onSubmit={save}><div className="section-head"><h2>Abrir caja</h2></div>{e&&<div className="error-state">{e}</div>}<div className="form-grid"><div className="field"><label>Base inicial *</label><input type="number" min="0" step="1" value={a} onChange={e=>setA(e.target.value)}/></div><div className="field field-full"><label>Notas</label><textarea rows={3} value={n} onChange={e=>setN(e.target.value)}/></div></div><div style={{display:'flex',justifyContent:'flex-end',gap:8,marginTop:16}}><button type="button" className="btn btn-ghost" onClick={onClose}>Cancelar</button><button className="btn btn-primary">Abrir caja</button></div></form>}
