"use client";import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';export default function SupplierSummary({supplierId}:{supplierId:string}){const s=createClient();const[d,setD]=useState({products:0});useEffect(()=>{(async()=>{const{count}=await s.from('inventory_products').select('id',{count:'exact',head:true}).eq('supplier_id',supplierId);setD({products:count??0})})()},[supplierId]);return <div className="grid grid-2"><div className="card"><div className="muted">Productos asociados</div><strong style={{fontSize:26}}>{d.products}</strong></div></div>}
