import { Code2, Database, LockKeyhole } from "lucide-react";
import { SimpleModule } from "@/components/simple-module";
export default function ApiDocs(){return <SimpleModule eyebrow="Integraciones" title="API" subtitle="Base preparada para integrar POS, aplicación móvil, portales y automatizaciones." icon="api"><div className="grid grid-3"><Box icon={<LockKeyhole/>} title="Auth" text="Supabase Auth + JWT / cookies SSR."/><Box icon={<Database/>} title="Datos" text="PostgreSQL + RLS multi-tenant."/><Box icon={<Code2/>} title="REST" text="Route Handlers /api/* y futura OpenAPI."/></div><div style={{height:16}}/><div className="card"><h2 style={{fontSize:16}}>Endpoints iniciales</h2><pre style={{background:"#0a0d0e",color:"#b8d9d1",padding:16,borderRadius:10,fontSize:11,overflow:"auto"}}>{`GET  /api/health\nGET  /api/customers\nPOST /api/customers\nGET  /api/motorcycles/:id\nPOST /api/service-orders\nPOST /api/quotes/:id/approve\nPOST /api/storage/vehicle-photo`}</pre><div className="footer-note">Los endpoints de negocio se implementan progresivamente con Server Actions / Route Handlers y políticas RLS.</div></div></SimpleModule>}
function Box({icon,title,text}:{icon:React.ReactNode,title:string,text:string}){return <div className="card"><div style={{color:"#0c6b58"}}>{icon}</div><h3>{title}</h3><p className="muted" style={{fontSize:12}}>{text}</p></div>}
